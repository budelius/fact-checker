---
phase: 03-ground-truth-discovery-and-paper-processing
plan: "06"
type: execute
wave: 4
depends_on: ["03", "05"]
files_modified:
  - backend/app/api/ground_truth.py
  - backend/app/main.py
  - backend/app/ground_truth/pipeline.py
  - backend/tests/test_ground_truth_api.py
  - backend/tests/test_ground_truth_e2e.py
  - docs/phase-3-ground-truth.md
  - AGENTS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
  - PPR-01
  - PPR-02
  - PPR-03
  - PPR-04
  - PPR-05
user_setup: []
must_haves:
  truths:
    - Phase 3 can be triggered from a completed Phase 2 ingestion job UUID.
    - The full fixture path produces selected/rejected/supplemental decisions, paper metadata, chunks, Markdown, and indexing calls.
    - Phase 3 still does not assign supported/contradicted/mixed/insufficient labels.
    - Requirements are marked complete only after backend tests pass.
  artifacts:
    - Ground-truth API router is wired into FastAPI.
    - E2E fixture test covers discovery through persistence/indexing without live provider calls.
    - Phase 3 docs describe routes, provider policy, paper-only ground truth, trace logs, and out-of-scope evaluation.
  key_links:
    - Depends on Plans 03 and 05.
    - Hands off to Phase 4 evaluation and report UI.
---

<objective>
Wire the full Phase 3 pipeline into the API and verify the end-to-end fixture path.

Purpose: contracts, clients, discovery, parsing, persistence, and indexing must be callable as one backend workflow before Phase 4 can evaluate evidence.
Output: API router, orchestration pipeline, E2E tests, docs, and planning handoff updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md
@docs/phase-2-ingestion.md
@backend/app/api/ingestion.py
@backend/app/main.py
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| API route runs live providers in tests | high | E2E tests inject fake providers, fake embedding provider, fake repositories, and temporary vault root. |
| Phase 3 route mutates Phase 2 claim truth labels | high | Tests assert claim `evidence_status` remains `pending` and no Phase 4 labels are emitted. |
| A failed provider call hides partial trace data | medium | Pipeline records failed/skipped stage events and returns recoverable job status. |
| Requirement check is marked complete without full verification | high | Requirements update task runs only after full backend test suite passes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add ground-truth pipeline orchestration</name>
  <files>backend/app/ground_truth/pipeline.py, backend/tests/test_ground_truth_e2e.py</files>
  <read_first>backend/app/ground_truth/discovery.py, backend/app/ground_truth/acquisition.py, backend/app/ground_truth/parsing.py, backend/app/ground_truth/chunking.py, backend/app/ground_truth/summarization.py, backend/app/ground_truth/persistence.py, backend/app/ground_truth/indexing.py, backend/app/ingestion/jobs.py</read_first>
  <action>Create `backend/app/ground_truth/pipeline.py` with `GroundTruthPipeline` that composes discovery service, acquisition, parsing, chunking, summarization, persistence, and indexing. Add `run_from_ingestion_payload(ingestion_payload: dict, repository, qdrant_repository, vault_root: Path) -> GroundTruthJob`. It must run discovery, process only candidates with selected ground-truth decisions, acquire/link papers, parse and chunk downloaded papers, create or fallback-generate summaries, persist Markdown/MongoDB records, index chunks, and preserve decisions for rejected/supplemental/no-paper candidates. If no selected papers exist, it must return a succeeded job with current operation `No scientific evidence found for now.` and no chunks. Add E2E tests using fake providers/repositories and a temporary vault root.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_e2e.py -q</verify>
  <acceptance_criteria>
    - `backend/app/ground_truth/pipeline.py` contains `class GroundTruthPipeline`
    - `backend/app/ground_truth/pipeline.py` contains `run_from_ingestion_payload`
    - `backend/app/ground_truth/pipeline.py` contains `No scientific evidence found for now.`
    - `backend/tests/test_ground_truth_e2e.py` contains `test_ground_truth_pipeline_processes_selected_paper_fixture`
    - `backend/tests/test_ground_truth_e2e.py` contains `test_ground_truth_pipeline_handles_no_paper_found`
    - `cd backend && uv run pytest tests/test_ground_truth_e2e.py -q` exits 0
  </acceptance_criteria>
  <done>The full Phase 3 pipeline can run from a Phase 2 ingestion payload with fake providers.</done>
</task>

<task type="auto">
  <name>Task 2: Add API routes for Phase 3 jobs</name>
  <files>backend/app/api/ground_truth.py, backend/app/main.py, backend/tests/test_ground_truth_api.py</files>
  <read_first>backend/app/api/ingestion.py, backend/app/main.py, backend/app/ingestion/jobs.py, backend/app/ground_truth/pipeline.py, backend/app/schemas/ground_truth.py</read_first>
  <action>Create `backend/app/api/ground_truth.py` with `APIRouter(prefix="/ground-truth", tags=["ground-truth"])`. Add `POST /ground-truth/jobs/from-ingestion/{ingestion_job_uuid}` that loads the ingestion job through `get_job`, returns 404 detail `ingestion_job_not_found` when missing, returns 400 detail `ingestion_job_has_no_claims` when the job has no claims, runs the pipeline, stores the result in an in-memory Phase 3 job store matching the current ingestion job pattern, and returns the serialized `GroundTruthJob`. Add `GET /ground-truth/jobs/{job_uuid}` returning the stored job or 404 detail `ground_truth_job_not_found`. Include the router in `backend/app/main.py`. Add API tests that first create a fixture transcript ingestion job, then trigger Phase 3 with monkeypatched/fake pipeline and assert response fields, decisions, pending claim evidence status, and 404 behavior.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_api.py -q</verify>
  <acceptance_criteria>
    - `backend/app/api/ground_truth.py` contains `router = APIRouter(prefix="/ground-truth"`
    - `backend/app/api/ground_truth.py` contains `/jobs/from-ingestion/{ingestion_job_uuid}`
    - `backend/app/api/ground_truth.py` contains `ground_truth_job_not_found`
    - `backend/app/main.py` contains `ground_truth_router`
    - `backend/tests/test_ground_truth_api.py` contains `test_start_ground_truth_job_from_ingestion_fixture`
    - `backend/tests/test_ground_truth_api.py` contains `test_missing_ground_truth_job_returns_404`
    - `cd backend && uv run pytest tests/test_ground_truth_api.py -q` exits 0
  </acceptance_criteria>
  <done>FastAPI can start and fetch Phase 3 ground-truth jobs from Phase 2 ingestion jobs.</done>
</task>

<task type="auto">
  <name>Task 3: Add E2E verification, docs, and Phase 4 handoff</name>
  <files>backend/tests/test_ground_truth_e2e.py, docs/phase-3-ground-truth.md, AGENTS.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>backend/tests/test_ingestion_e2e.py, docs/phase-2-ingestion.md, AGENTS.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Extend `backend/tests/test_ground_truth_e2e.py` to cover the API-level fixture path: create a pasted transcript containing `arXiv:1706.03762`, run the Phase 3 route with fake providers returning one selected paper and one supplemental non-paper source, assert the result includes selected/rejected/supplemental decisions, a paper Markdown vault path, at least one paper chunk, and one Qdrant indexing call, and assert no claim label values `supported`, `contradicted`, `mixed`, or `insufficient` appear in the Phase 3 response. Add `docs/phase-3-ground-truth.md` documenting scope, API routes, provider policy, paper/preprint-only ground truth, no-scientific-evidence wording, lawful PDF behavior, Markdown outputs, MongoDB/Qdrant traceability, and out-of-scope Phase 4 evaluation. Update `AGENTS.md` current focus to Phase 3 completion/handoff and add a `Phase 3 Contract Files` section. After `cd backend && uv run pytest -q` passes, mark `SRC-01` to `SRC-05` and `PPR-01` to `PPR-05` complete in `.planning/REQUIREMENTS.md`, update `.planning/ROADMAP.md` Phase 3 status to complete, and update `.planning/STATE.md` so current focus is Phase 4 ready to discuss.</action>
  <verify>cd backend && uv run pytest -q</verify>
  <acceptance_criteria>
    - `docs/phase-3-ground-truth.md` contains `papers and preprints only`
    - `docs/phase-3-ground-truth.md` contains `No scientific evidence found for now`
    - `docs/phase-3-ground-truth.md` contains `POST /ground-truth/jobs/from-ingestion`
    - `AGENTS.md` contains `Phase 3 Contract Files`
    - `.planning/REQUIREMENTS.md` contains `[x] **SRC-01**`
    - `.planning/REQUIREMENTS.md` contains `[x] **PPR-05**`
    - `.planning/ROADMAP.md` contains `Phase 3: Ground-Truth Discovery and Paper Processing`
    - `.planning/STATE.md` contains `Current phase: Phase 4`
    - `cd backend && uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>Phase 3 is documented, verified, requirements-traced, and handed off to Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring phase planned/complete:
- [ ] `cd backend && uv run pytest tests/test_ground_truth_api.py tests/test_ground_truth_e2e.py -q`
- [ ] `cd backend && uv run pytest -q`
- [ ] `grep -q "POST /ground-truth/jobs/from-ingestion" docs/phase-3-ground-truth.md`
- [ ] `grep -q "Phase 3 Contract Files" AGENTS.md`
- [ ] `grep -Fq "[x] **SRC-01**" .planning/REQUIREMENTS.md`
- [ ] `grep -Fq "[x] **PPR-05**" .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- Phase 3 can be triggered from a completed Phase 2 ingestion job.
- The E2E fixture path produces paper candidates, source decisions, paper metadata, chunks, Markdown, and Qdrant payloads without live provider calls.
- Non-paper sources remain supplemental.
- No-paper cases produce `No scientific evidence found for now.`
- Phase 3 docs and project state hand off to Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-discovery-and-paper-processing/03-06-SUMMARY.md`.
</output>
