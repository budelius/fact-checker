---
phase: 03-ground-truth-discovery-and-paper-processing
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/ground_truth/__init__.py
  - backend/app/ground_truth/clients/__init__.py
  - backend/app/ground_truth/clients/arxiv.py
  - backend/app/ground_truth/clients/openalex.py
  - backend/app/ground_truth/clients/semantic_scholar.py
  - backend/app/ground_truth/clients/openai_search.py
  - backend/app/ground_truth/queries.py
  - backend/tests/test_ground_truth_clients.py
  - backend/tests/fixtures/ground_truth/arxiv_attention.xml
  - backend/tests/fixtures/ground_truth/openalex_attention.json
  - backend/tests/fixtures/ground_truth/semantic_scholar_attention.json
  - backend/tests/fixtures/ground_truth/openai_web_search_attention.json
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-04
  - SRC-05
user_setup: []
must_haves:
  truths:
    - Provider clients return normalized `PaperCandidate` objects and never select ground truth themselves.
    - OpenAI web search citations and sources are preserved as discovery paths and raw trace metadata.
    - Tests do not call live OpenAI, arXiv, OpenAlex, or Semantic Scholar APIs.
  artifacts:
    - Provider clients exist under `backend/app/ground_truth/clients/`.
    - Query generation produces exact identifier, title, claim, transcript, and screenshot/source-clue query variants.
    - Fixture tests cover all four provider paths.
  key_links:
    - Provider output feeds Plan 03 dedupe and selection.
---

<objective>
Implement source discovery provider clients and query generation.

Purpose: Phase 3 needs isolated, mockable adapters for OpenAI live web search and scholarly indexes before the orchestrator can merge candidates or enforce ground-truth policy.
Output: provider clients, query builder, fixtures, and client tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-PATTERNS.md
@backend/app/schemas/ground_truth.py
@backend/app/settings.py
@backend/app/ingestion/research_basis.py
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Live API calls run in tests | high | All tests use fixtures and fake transports; provider constructors accept injected clients/transports. |
| OpenAI web search prose is treated as ground truth | high | OpenAI client returns candidates and citations only; selection happens in Plan 03. |
| API keys leak into trace metadata | high | Strip request headers and key values from stored raw provider metadata. |
| Provider parsing drops citations/source URLs | medium | Tests assert discovery paths contain provider, query, rank, and URL for results. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add query generation from Phase 2 hints and claims</name>
  <files>backend/app/ground_truth/__init__.py, backend/app/ground_truth/queries.py, backend/tests/test_ground_truth_clients.py</files>
  <read_first>backend/app/ingestion/research_basis.py, backend/app/schemas/claims.py, backend/app/schemas/ingestion.py, backend/app/schemas/ground_truth.py, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md</read_first>
  <action>Create `backend/app/ground_truth/queries.py` with `DiscoveryQuery` Pydantic model or dataclass containing `query`, `provider_hint`, `source_candidate_uuid`, `claim_uuid`, and `reason`. Add `build_discovery_queries(claims, research_basis, screenshots=None) -> list[DiscoveryQuery]`. It must generate exact queries for DOI values, exact queries for arXiv values, URL queries for URLs, quoted title queries for `paper_title` candidates, semantic claim queries from `ExtractedClaim.claim_text`, transcript excerpt queries from `ExtractedClaim.transcript_excerpt`, and screenshot/source-clue queries from screenshot `source_clue_text`. Deduplicate identical query strings while preserving all reasons in deterministic order. Add tests named `test_build_discovery_queries_prefers_identifiers` and `test_build_discovery_queries_adds_claim_and_screenshot_queries`.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_clients.py -q</verify>
  <acceptance_criteria>
    - `backend/app/ground_truth/queries.py` contains `build_discovery_queries`
    - `backend/app/ground_truth/queries.py` contains `doi`
    - `backend/app/ground_truth/queries.py` contains `arxiv`
    - `backend/app/ground_truth/queries.py` contains `source_clue_text`
    - `backend/tests/test_ground_truth_clients.py` contains `test_build_discovery_queries_prefers_identifiers`
    - `backend/tests/test_ground_truth_clients.py` contains `test_build_discovery_queries_adds_claim_and_screenshot_queries`
    - `cd backend && uv run pytest tests/test_ground_truth_clients.py -q` exits 0
  </acceptance_criteria>
  <done>Discovery query generation is deterministic and traceable.</done>
</task>

<task type="auto">
  <name>Task 2: Add arXiv, OpenAlex, and Semantic Scholar clients</name>
  <files>backend/app/ground_truth/clients/__init__.py, backend/app/ground_truth/clients/arxiv.py, backend/app/ground_truth/clients/openalex.py, backend/app/ground_truth/clients/semantic_scholar.py, backend/tests/test_ground_truth_clients.py, backend/tests/fixtures/ground_truth/arxiv_attention.xml, backend/tests/fixtures/ground_truth/openalex_attention.json, backend/tests/fixtures/ground_truth/semantic_scholar_attention.json</files>
  <read_first>backend/app/settings.py, backend/app/schemas/ground_truth.py, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md</read_first>
  <action>Create three clients returning `list[PaperCandidate]`. `ArxivClient.search(query: DiscoveryQuery)` must call or build requests for `https://export.arxiv.org/api/query` using `id_list` for arXiv identifiers and `search_query` for search terms, parse Atom XML, and map entries to `SourceProvider.arxiv` discovery paths. `OpenAlexClient.search(query: DiscoveryQuery)` must call or build requests for `https://api.openalex.org/works`, use DOI filtering for DOI queries, `search` for text queries, `select` fields `id,doi,display_name,publication_date,publication_year,authorships,open_access,primary_location,locations,is_retracted,type,referenced_works`, and map results to `SourceProvider.openalex`. `SemanticScholarClient.search(query: DiscoveryQuery)` must call or build requests for `https://api.semanticscholar.org/graph/v1/paper/search/bulk` with fields `paperId,url,title,abstract,authors,year,publicationDate,publicationTypes,openAccessPdf,citationCount,externalIds`, include `x-api-key` only when configured, and map results to `SourceProvider.semantic_scholar`. Add fixture files with one Attention-is-All-You-Need-style result per provider. Add tests that parse each fixture and assert title, provider, external IDs, pdf/landing URLs, and discovery path rank.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_clients.py -q</verify>
  <acceptance_criteria>
    - `backend/app/ground_truth/clients/arxiv.py` contains `class ArxivClient`
    - `backend/app/ground_truth/clients/arxiv.py` contains `export.arxiv.org/api/query`
    - `backend/app/ground_truth/clients/openalex.py` contains `class OpenAlexClient`
    - `backend/app/ground_truth/clients/openalex.py` contains `api.openalex.org/works`
    - `backend/app/ground_truth/clients/semantic_scholar.py` contains `class SemanticScholarClient`
    - `backend/app/ground_truth/clients/semantic_scholar.py` contains `graph/v1/paper/search/bulk`
    - `backend/tests/fixtures/ground_truth/arxiv_attention.xml` contains `Attention Is All You Need`
    - `backend/tests/test_ground_truth_clients.py` contains `test_arxiv_client_maps_fixture`
    - `backend/tests/test_ground_truth_clients.py` contains `test_openalex_client_maps_fixture`
    - `backend/tests/test_ground_truth_clients.py` contains `test_semantic_scholar_client_maps_fixture`
    - `cd backend && uv run pytest tests/test_ground_truth_clients.py -q` exits 0
  </acceptance_criteria>
  <done>Paper-index clients normalize arXiv, OpenAlex, and Semantic Scholar results.</done>
</task>

<task type="auto">
  <name>Task 3: Add OpenAI web search client with citations and sources</name>
  <files>backend/app/ground_truth/clients/openai_search.py, backend/tests/test_ground_truth_clients.py, backend/tests/fixtures/ground_truth/openai_web_search_attention.json</files>
  <read_first>backend/app/settings.py, backend/app/safety/input_boundaries.py, backend/app/schemas/ground_truth.py, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md</read_first>
  <action>Create `OpenAIWebSearchClient.search(query: DiscoveryQuery) -> list[PaperCandidate]`. The real call should use the OpenAI Responses API with the configured `openai_discovery_model`, a `web_search` tool, `include=["web_search_call.action.sources"]`, and a prompt that asks for paper/preprint candidates only. The client must parse citation annotations and sources into `DiscoveryPath` entries with `SourceProvider.openai_web`. Add a fixture JSON representing a Responses API result with an arXiv citation URL and a `sources` list. Tests must instantiate the client in fixture mode or call a pure parser and assert that citations and complete source URLs are preserved, non-paper URLs map to `CandidateKind.non_paper` or supplemental candidates, and no API key appears in returned raw provider data.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_clients.py -q</verify>
  <acceptance_criteria>
    - `backend/app/ground_truth/clients/openai_search.py` contains `class OpenAIWebSearchClient`
    - `backend/app/ground_truth/clients/openai_search.py` contains `web_search`
    - `backend/app/ground_truth/clients/openai_search.py` contains `web_search_call.action.sources`
    - `backend/app/ground_truth/clients/openai_search.py` contains `url_citation`
    - `backend/tests/fixtures/ground_truth/openai_web_search_attention.json` contains `sources`
    - `backend/tests/test_ground_truth_clients.py` contains `test_openai_web_search_preserves_citations_and_sources`
    - `backend/tests/test_ground_truth_clients.py` contains `test_openai_web_search_does_not_leak_api_key`
    - `cd backend && uv run pytest tests/test_ground_truth_clients.py -q` exits 0
  </acceptance_criteria>
  <done>OpenAI live-search results preserve citations and full source traces without selecting truth.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && uv run pytest tests/test_ground_truth_clients.py -q`
- [ ] `grep -q "class ArxivClient" backend/app/ground_truth/clients/arxiv.py`
- [ ] `grep -q "class OpenAlexClient" backend/app/ground_truth/clients/openalex.py`
- [ ] `grep -q "class SemanticScholarClient" backend/app/ground_truth/clients/semantic_scholar.py`
- [ ] `grep -q "class OpenAIWebSearchClient" backend/app/ground_truth/clients/openai_search.py`
- [ ] `grep -q "web_search_call.action.sources" backend/app/ground_truth/clients/openai_search.py`
</verification>

<success_criteria>
- Query generation uses identifiers, claims, transcript excerpts, and source-clue text.
- Provider clients normalize OpenAI web search, arXiv, OpenAlex, and Semantic Scholar results into candidate records.
- Provider tests are deterministic and do not use live network calls.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-discovery-and-paper-processing/03-02-SUMMARY.md`.
</output>
