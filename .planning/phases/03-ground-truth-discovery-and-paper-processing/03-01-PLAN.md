---
phase: 03-ground-truth-discovery-and-paper-processing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/settings.py
  - backend/app/contracts/logging.py
  - backend/app/contracts/vault.py
  - backend/app/schemas/ground_truth.py
  - backend/app/schemas/__init__.py
  - backend/tests/test_ground_truth_schemas.py
  - backend/tests/test_contracts.py
  - backend/tests/test_settings.py
  - vault/SCHEMA.md
autonomous: true
requirements:
  - SRC-03
  - SRC-04
  - SRC-05
  - PPR-01
  - PPR-02
  - PPR-03
  - PPR-04
  - PPR-05
user_setup: []
must_haves:
  truths:
    - Phase 3 has typed source discovery, paper candidate, paper metadata, processing, chunk, and summary records before provider logic starts.
    - Candidate status can represent selected ground-truth paper, rejected candidate, supplemental non-paper source, no paper found, and needs manual review.
    - Raw paper artifacts have an explicit vault contract.
  artifacts:
    - `backend/app/schemas/ground_truth.py` defines the shared Phase 3 DTOs and enums.
    - `backend/app/settings.py` contains provider and model configuration without committed secrets.
    - `backend/app/contracts/logging.py` accepts Phase 3 trace event types.
    - `backend/app/contracts/vault.py` and `vault/SCHEMA.md` include `papers` under raw artifacts.
  key_links:
    - `ResearchBasisCandidate` from Phase 2 remains input, not evaluated evidence.
    - External IDs stay metadata/aliases; UUIDs stay canonical.
---

<objective>
Create the Phase 3 contract foundation.

Purpose: all later source-discovery, paper-processing, persistence, and API tasks need stable schemas, settings, logging vocabulary, and vault path contracts before they touch provider APIs or storage.
Output: dependencies, settings, Pydantic DTOs, raw paper vault contract, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md
@.planning/phases/03-ground-truth-discovery-and-paper-processing/03-PATTERNS.md
@docs/phase-1-contracts.md
@docs/phase-2-ingestion.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Non-paper source becomes v1 ground truth | high | Add `CandidateStatus` and `CandidateKind` enums that separate selected paper/preprint from supplemental non-paper. |
| Provider secrets leak into Markdown or trace events | high | Add settings fields only; never store API key values in DTOs, logs, tests, or docs. |
| Later tasks cannot trace chunks to paper/source UUIDs | high | Add chunk and metadata schemas with UUID, source UUID, vault path, external IDs, and provenance fields. |
| Existing logging contract rejects Phase 3 events | medium | Extend event types with Phase 3 names and test them. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 runtime dependencies and settings</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/app/settings.py, backend/tests/test_settings.py</files>
  <read_first>backend/pyproject.toml, backend/app/settings.py, backend/tests/test_settings.py, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-RESEARCH.md</read_first>
  <action>Run `uv add httpx openai pypdf` from `backend/` so `backend/pyproject.toml` and `backend/uv.lock` include runtime dependencies for HTTP provider clients, OpenAI Responses/Embeddings calls, and PDF parsing. Update `Settings` in `backend/app/settings.py` with optional provider/model fields: `openai_discovery_model: str = Field(default="gpt-5.4-mini", alias="OPENAI_DISCOVERY_MODEL")`, `openai_summary_model: str = Field(default="gpt-5.4-mini", alias="OPENAI_SUMMARY_MODEL")`, `openai_embedding_model: str = Field(default="text-embedding-3-small", alias="OPENAI_EMBEDDING_MODEL")`, `openai_embedding_dimensions: int | None = Field(default=None, alias="OPENAI_EMBEDDING_DIMENSIONS")`, `openalex_email: str | None = Field(default=None, alias="OPENALEX_EMAIL")`, `semantic_scholar_api_key: str | None = Field(default=None, alias="SEMANTIC_SCHOLAR_API_KEY")`, `ground_truth_max_results_per_provider: int = Field(default=10, alias="GROUND_TRUTH_MAX_RESULTS_PER_PROVIDER")`, `paper_download_enabled: bool = Field(default=True, alias="PAPER_DOWNLOAD_ENABLED")`, `paper_max_pdf_mb: int = Field(default=40, alias="PAPER_MAX_PDF_MB")`, and `paper_request_timeout_seconds: float = Field(default=20.0, alias="PAPER_REQUEST_TIMEOUT_SECONDS")`. Extend `backend/tests/test_settings.py` with a test named `test_ground_truth_settings_defaults` asserting the defaults above and a test named `test_ground_truth_settings_from_env` asserting env overrides for `OPENAI_DISCOVERY_MODEL`, `OPENAI_EMBEDDING_MODEL`, `OPENALEX_EMAIL`, `SEMANTIC_SCHOLAR_API_KEY`, and `PAPER_DOWNLOAD_ENABLED`.</action>
  <verify>cd backend && uv run pytest tests/test_settings.py -q</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `"httpx"`
    - `backend/pyproject.toml` contains `"openai"`
    - `backend/pyproject.toml` contains `"pypdf"`
    - `backend/app/settings.py` contains `OPENAI_DISCOVERY_MODEL`
    - `backend/app/settings.py` contains `OPENAI_EMBEDDING_MODEL`
    - `backend/app/settings.py` contains `SEMANTIC_SCHOLAR_API_KEY`
    - `backend/app/settings.py` contains `PAPER_MAX_PDF_MB`
    - `backend/tests/test_settings.py` contains `test_ground_truth_settings_defaults`
    - `cd backend && uv run pytest tests/test_settings.py -q` exits 0
  </acceptance_criteria>
  <done>Provider, embedding, and paper acquisition configuration exists without hard-coded secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Add ground-truth schemas</name>
  <files>backend/app/schemas/ground_truth.py, backend/app/schemas/__init__.py, backend/tests/test_ground_truth_schemas.py</files>
  <read_first>backend/app/schemas/ingestion.py, backend/app/schemas/claims.py, backend/app/schemas/entities.py, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md</read_first>
  <action>Create `backend/app/schemas/ground_truth.py` with Pydantic v2 models and enums. Define `GroundTruthStageName` values exactly `load_claims`, `generate_queries`, `search_openai_web`, `search_paper_indexes`, `merge_candidates`, `select_sources`, `acquire_papers`, `parse_papers`, `summarize_papers`, `index_chunks`, `write_owned_artifacts`. Define `SourceProvider` values exactly `phase2_hint`, `openai_web`, `arxiv`, `openalex`, `semantic_scholar`. Define `CandidateKind` values exactly `paper`, `preprint`, `non_paper`, `unknown`. Define `CandidateStatus` values exactly `selected_ground_truth`, `rejected`, `supplemental`, `no_paper_found`, `needs_manual_review`. Define `PaperProcessingStatus` values exactly `metadata_only`, `linked`, `downloaded`, `parsed`, `chunked`, `summarized`, `indexed`, `failed`. Add models `ExternalPaperId(provider: str, value: str)`, `DiscoveryPath(provider: SourceProvider, query: str | None, source_candidate_uuid: UUID | None, result_rank: int | None, url: str | None, note: str | None)`, `GroundTruthStage(name: GroundTruthStageName, status: str, message: str | None, event_uuid: UUID | None, started_at: datetime | None, completed_at: datetime | None)`, `GroundTruthArtifact(uuid: UUID, artifact_type: str, vault_path: str | None, source_url: str | None, metadata: dict[str, Any])`, `PaperAuthor(name: str, external_ids: list[ExternalPaperId])`, `PaperCandidate(uuid, title, kind, status, external_ids, authors, abstract, publication_date, source_url, pdf_url, landing_page_url, confidence, discovery_paths, raw_provider_data, selected_reason, rejected_reason)`, `SourceDecision(uuid, claim_uuid, candidate_uuid, status, reason, provenance)`, `PaperMetadata(uuid, title, authors, external_ids, publication_status, publication_date, abstract, source_links, pdf_url, vault_path, processing_status)`, `PaperAcquisition(uuid, paper_uuid, source_url, pdf_url, raw_pdf_path, status, reason, bytes_downloaded)`, `PaperChunk(uuid, paper_uuid, source_uuid, chunk_id, text, page_start, page_end, section, vault_path, source_url)`, `PaperSummary(uuid, paper_uuid, summary_markdown, methods, key_claims, limitations, references, provenance)`, and `GroundTruthJob(job_uuid, ingestion_job_uuid, status, current_operation, stages: list[GroundTruthStage], artifacts: list[GroundTruthArtifact], candidates, decisions, papers, chunks, created_at, updated_at, error_message)`. Use `Field(default_factory=list)` for lists and `uuid4` for UUID defaults. Export the module from `backend/app/schemas/__init__.py`. Add tests asserting all enum values, that `CandidateStatus` contains `no_paper_found`, that `GroundTruthJob` defaults `stages` and `artifacts` to empty lists, and that `PaperChunk` requires `paper_uuid`, `source_uuid`, `chunk_id`, `text`, and `vault_path`.</action>
  <verify>cd backend && uv run pytest tests/test_ground_truth_schemas.py -q</verify>
  <acceptance_criteria>
    - `backend/app/schemas/ground_truth.py` contains `class GroundTruthStageName`
    - `backend/app/schemas/ground_truth.py` contains `class SourceProvider`
    - `backend/app/schemas/ground_truth.py` contains `class GroundTruthStage`
    - `backend/app/schemas/ground_truth.py` contains `class GroundTruthArtifact`
    - `backend/app/schemas/ground_truth.py` contains `selected_ground_truth`
    - `backend/app/schemas/ground_truth.py` contains `no_paper_found`
    - `backend/app/schemas/ground_truth.py` contains `class PaperCandidate`
    - `backend/app/schemas/ground_truth.py` contains `class SourceDecision`
    - `backend/app/schemas/ground_truth.py` contains `class PaperMetadata`
    - `backend/app/schemas/ground_truth.py` contains `class PaperChunk`
    - `backend/app/schemas/ground_truth.py` contains `class GroundTruthJob`
    - `backend/tests/test_ground_truth_schemas.py` contains `test_candidate_status_values`
    - `cd backend && uv run pytest tests/test_ground_truth_schemas.py -q` exits 0
  </acceptance_criteria>
  <done>Phase 3 has typed DTOs for provider candidates, decisions, paper metadata, acquisition, chunks, summaries, and jobs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend logging and vault contracts for paper processing</name>
  <files>backend/app/contracts/logging.py, backend/app/contracts/vault.py, backend/tests/test_contracts.py, vault/SCHEMA.md</files>
  <read_first>backend/app/contracts/logging.py, backend/app/contracts/vault.py, backend/tests/test_contracts.py, vault/SCHEMA.md, .planning/phases/03-ground-truth-discovery-and-paper-processing/03-CONTEXT.md</read_first>
  <action>Extend `PIPELINE_EVENT_TYPES` in `backend/app/contracts/logging.py` with `source_discovery`, `paper_processing`, and `summarization` while preserving existing values `ingestion`, `search`, `parsing`, `evaluation`, `indexing`, and `graph_write`. Update `backend/tests/test_contracts.py` to assert the three new event types and to build a valid event with `event_type="source_discovery"` and `stage="merge_candidates"`. Extend `VAULT_RAW_ARTIFACT_FOLDERS` in `backend/app/contracts/vault.py` to include `papers`. Update `backend/tests/test_contracts.py` so `test_vault_folders_match_contract` expects `("videos", "transcripts", "screenshots", "papers")` for raw artifacts and add `test_raw_artifact_path_for_paper_pdf` asserting `expected_raw_artifact_path("papers", "attention-is-all-you-need", ".pdf") == "vault/raw/papers/attention-is-all-you-need.pdf"`. Update `vault/SCHEMA.md` to list `vault/raw/papers/` and state that paper PDFs are immutable raw evidence inputs created only from lawful public access.</action>
  <verify>cd backend && uv run pytest tests/test_contracts.py -q</verify>
  <acceptance_criteria>
    - `backend/app/contracts/logging.py` contains `"source_discovery"`
    - `backend/app/contracts/logging.py` contains `"paper_processing"`
    - `backend/app/contracts/logging.py` contains `"summarization"`
    - `backend/app/contracts/vault.py` contains `"papers"`
    - `backend/tests/test_contracts.py` contains `test_raw_artifact_path_for_paper_pdf`
    - `vault/SCHEMA.md` contains `vault/raw/papers/`
    - `vault/SCHEMA.md` contains `lawful public access`
    - `cd backend && uv run pytest tests/test_contracts.py -q` exits 0
  </acceptance_criteria>
  <done>Trace logging and vault raw-paper storage contracts support Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && uv run pytest tests/test_settings.py tests/test_contracts.py tests/test_ground_truth_schemas.py -q`
- [ ] `grep -q "class PaperCandidate" backend/app/schemas/ground_truth.py`
- [ ] `grep -q "selected_ground_truth" backend/app/schemas/ground_truth.py`
- [ ] `grep -q "source_discovery" backend/app/contracts/logging.py`
- [ ] `grep -q "vault/raw/papers/" vault/SCHEMA.md`
</verification>

<success_criteria>
- Runtime dependencies and configuration support Phase 3 provider clients and paper parsing.
- Ground-truth schemas cover stage progress, artifacts, candidate status, source decisions, paper metadata, paper acquisition, chunks, summaries, and jobs.
- Logging and vault contracts explicitly support trace-heavy paper processing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-discovery-and-paper-processing/03-01-SUMMARY.md`.
</output>
