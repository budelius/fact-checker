---
phase: 04-evidence-evaluation-and-fact-check-reports
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/evaluation.py
  - backend/app/schemas/claims.py
  - backend/app/schemas/__init__.py
  - backend/app/settings.py
  - backend/tests/test_evaluation_schemas.py
  - backend/tests/test_settings.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
user_setup: []
must_haves:
  truths:
    - Phase 4 labels live in evaluation/report payloads and do not break Phase 2/3 pending claim defaults.
    - Every evaluation artifact uses UUID identity.
    - The schema can represent cited evidence, unused candidate evidence, subclaims, overclaim notes, uncertainty notes, source limits, preprint status, report versions, and rerun availability.
  artifacts:
    - Evaluation schemas exist and serialize cleanly through FastAPI/Pydantic.
    - Settings include a configurable OpenAI evaluation model.
    - Tests cover allowed labels and invalid label rejection.
  key_links:
    - Feeds Plans 02, 03, 04, and 05.
---

<objective>
Create the Phase 4 evaluation contracts before adding evaluator, persistence, API, or UI code.

Purpose: later plans need stable shared shapes for labels, citations, report versions, progress stages, and validation failures.
Output: Pydantic schemas, settings, and schema tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evidence-evaluation-and-fact-check-reports/04-CONTEXT.md
@.planning/phases/04-evidence-evaluation-and-fact-check-reports/04-RESEARCH.md
@.planning/phases/04-evidence-evaluation-and-fact-check-reports/04-PATTERNS.md
@backend/app/schemas/claims.py
@backend/app/schemas/ground_truth.py
@backend/app/schemas/ingestion.py
@backend/app/settings.py
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Label semantics drift across backend and frontend | high | Add a single backend enum and later mirror it in the frontend API type. |
| Phase 4 labels leak into Phase 2/3 defaults too early | medium | Keep `ExtractedClaim.evidence_status` default as `pending`; report payloads own evaluated labels. |
| Missing citation/provenance fields force ad hoc data later | high | Add explicit citation, candidate evidence, validation error, report version, and job schemas now. |
| Report version identity becomes ambiguous | medium | Include report UUID, report group UUID, version, markdown path, created timestamp, and source job UUIDs. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add evaluation schema module</name>
  <files>backend/app/schemas/evaluation.py, backend/app/schemas/__init__.py, backend/tests/test_evaluation_schemas.py</files>
  <read_first>backend/app/schemas/ground_truth.py, backend/app/schemas/claims.py, backend/app/schemas/entities.py, backend/app/schemas/relationships.py</read_first>
  <action>Create `backend/app/schemas/evaluation.py`. Define `EvaluationLabel` with exactly `supported`, `contradicted`, `mixed`, and `insufficient`. Define stage/status models for report generation using existing `JobLifecycleStatus` and `StageStatus`. Add models for `EvidenceCitation`, `EvidenceCandidate`, `SubclaimEvaluation`, `ClaimEvaluation`, `EvaluationValidationError`, `ReportVersion`, and `EvaluationJob`. Include fields for claim UUIDs, evidence UUIDs, paper/source UUIDs, chunk IDs, source URLs, page/section, raw excerpt text, publication/preprint status, source policy notes, rationale, uncertainty notes, overclaim notes, label counts, Markdown path, cited and unused candidates, report UUIDs, report group UUID, version, and timestamps. Use UUID defaults where new records are created. Export the module from `backend/app/schemas/__init__.py` only if this repo already uses package exports; otherwise leave imports direct.</action>
  <verify>cd backend && uv run pytest tests/test_evaluation_schemas.py -q</verify>
  <acceptance_criteria>
    - `backend/app/schemas/evaluation.py` contains `class EvaluationLabel`
    - `backend/app/schemas/evaluation.py` contains `supported = "supported"`
    - `backend/app/schemas/evaluation.py` contains `contradicted = "contradicted"`
    - `backend/app/schemas/evaluation.py` contains `mixed = "mixed"`
    - `backend/app/schemas/evaluation.py` contains `insufficient = "insufficient"`
    - `backend/app/schemas/evaluation.py` contains `class ClaimEvaluation`
    - `backend/app/schemas/evaluation.py` contains `class ReportVersion`
    - `backend/app/schemas/evaluation.py` contains `class EvaluationJob`
    - `backend/tests/test_evaluation_schemas.py` asserts invalid labels fail validation
    - `cd backend && uv run pytest tests/test_evaluation_schemas.py -q` exits 0
  </acceptance_criteria>
  <done>Phase 4 has explicit typed evaluation and report payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve claim defaults while allowing evaluated status mapping</name>
  <files>backend/app/schemas/claims.py, backend/tests/test_evaluation_schemas.py</files>
  <read_first>backend/app/schemas/claims.py, backend/tests/test_ground_truth_api.py, backend/tests/test_ground_truth_e2e.py</read_first>
  <action>Keep the existing `EvidenceStatus.pending` default for extracted claims. If implementation needs evaluated labels on claim records, extend the enum with Phase 4 label values while preserving `pending` as the default and without changing Phase 2/3 API payload behavior. Add tests proving a new `ExtractedClaim` still serializes with `evidence_status: pending` and that report-level labels are represented by `EvaluationLabel`.</action>
  <verify>cd backend && uv run pytest tests/test_evaluation_schemas.py tests/test_ground_truth_api.py tests/test_ground_truth_e2e.py -q</verify>
  <acceptance_criteria>
    - New `ExtractedClaim` objects still default to `EvidenceStatus.pending`
    - Phase 3 tests still assert no Phase 4 labels appear in Phase 3 responses
    - Evaluation labels are available through `EvaluationLabel`
    - `cd backend && uv run pytest tests/test_evaluation_schemas.py tests/test_ground_truth_api.py tests/test_ground_truth_e2e.py -q` exits 0
  </acceptance_criteria>
  <done>Phase 4 labeling contracts do not regress earlier phase payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add evaluation settings</name>
  <files>backend/app/settings.py, backend/tests/test_settings.py</files>
  <read_first>backend/app/settings.py, backend/app/ground_truth/summarization.py, backend/app/ground_truth/clients/openai_search.py</read_first>
  <action>Add settings for Phase 4 evaluation: `openai_evaluation_model` defaulting to the same conservative model family already used by discovery/summary, `evaluation_max_chunks_per_claim`, `evaluation_excerpt_max_chars`, and any needed timeout/retry values if existing settings already expose similar provider controls. Keep environment variable names predictable, such as `OPENAI_EVALUATION_MODEL`. Add tests or extend existing settings tests to assert defaults.</action>
  <verify>cd backend && uv run pytest tests/test_settings.py tests/test_evaluation_schemas.py -q</verify>
  <acceptance_criteria>
    - `backend/app/settings.py` contains `openai_evaluation_model`
    - `backend/app/settings.py` contains `evaluation_max_chunks_per_claim`
    - `backend/app/settings.py` contains `evaluation_excerpt_max_chars`
    - Settings tests assert the new defaults
    - `cd backend && uv run pytest tests/test_settings.py tests/test_evaluation_schemas.py -q` exits 0
  </acceptance_criteria>
  <done>Evaluator behavior is configurable without hard-coded model constants.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cd backend && uv run pytest tests/test_evaluation_schemas.py tests/test_settings.py -q`
- [ ] `cd backend && uv run pytest tests/test_ground_truth_api.py tests/test_ground_truth_e2e.py -q`
- [ ] `rg -n "class EvaluationLabel|class ClaimEvaluation|class ReportVersion|class EvaluationJob" backend/app/schemas/evaluation.py`
- [ ] `rg -n "openai_evaluation_model|evaluation_max_chunks_per_claim|evaluation_excerpt_max_chars" backend/app/settings.py`
</verification>

<success_criteria>
- Evaluation labels and report payloads are typed centrally.
- Earlier phase claim defaults stay pending.
- Later plans can consume report, citation, candidate evidence, validation, and stage schemas without inventing parallel shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-evaluation-and-fact-check-reports/04-01-SUMMARY.md`.
</output>
