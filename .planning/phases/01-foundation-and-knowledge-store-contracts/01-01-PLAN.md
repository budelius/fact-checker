---
phase: 01-foundation-and-knowledge-store-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - README.md
  - infra/docker-compose.yml
  - infra/README.md
autonomous: true
requirements:
  - OPS-01
  - OPS-03
user_setup: []
must_haves:
  truths:
    - Docker Compose default services are datastores only: MongoDB and Qdrant.
    - Secrets are represented as environment variable placeholders, not committed values.
  artifacts:
    - .env.example exists with MongoDB and Qdrant connection settings.
    - infra/docker-compose.yml exists with mongodb and qdrant services.
    - infra/README.md explains local datastore startup and shutdown.
  key_links:
    - README.md points developers to infra/README.md for local datastore setup.
---

<objective>
Create the local infrastructure foundation for Phase 1.

Purpose: Phase 1 needs a simple, reproducible local datastore setup without running app services in Docker. This plan creates the root environment contract and Docker Compose setup for MongoDB and Qdrant only.
Output: `.env.example`, `infra/docker-compose.yml`, `infra/README.md`, and README setup notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-RESEARCH.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Secrets committed in `.env.example` | high | Use placeholder values only and document real secrets stay in local `.env`, which remains ignored. |
| Datastore ports exposed unexpectedly | medium | Bind only documented local development ports: MongoDB `27017`, Qdrant REST `6333`, Qdrant gRPC `6334`. |
| Persistent local data accidentally removed | low | Document `docker compose down -v` as destructive in `infra/README.md`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add datastore-only Docker Compose</name>
  <files>infra/docker-compose.yml, infra/README.md</files>
  <read_first>.planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-RESEARCH.md</read_first>
  <action>Create `infra/docker-compose.yml` with Compose services named exactly `mongodb` and `qdrant`. `mongodb` must use image `mongodb/mongodb-community-server:latest`, expose `27017:27017`, set `MONGODB_INITDB_ROOT_USERNAME=${MONGODB_ROOT_USERNAME:-factchecker}` and `MONGODB_INITDB_ROOT_PASSWORD=${MONGODB_ROOT_PASSWORD:-factchecker-dev-password}`, and mount named volumes `mongodb_data:/data/db` and `mongodb_config:/data/configdb`. `qdrant` must use image `qdrant/qdrant:latest`, expose `6333:6333` and `6334:6334`, set `QDRANT__SERVICE__HTTP_PORT=6333`, and mount named volume `qdrant_storage:/qdrant/storage`. Do not add backend or frontend services to default Compose. Create `infra/README.md` documenting `docker compose --env-file ../.env.example up -d`, `docker compose ps`, `docker compose down`, and `docker compose down -v` with a warning that `-v` deletes local datastore volumes.</action>
  <verify>grep -q "mongodb/mongodb-community-server:latest" infra/docker-compose.yml && grep -q "qdrant/qdrant:latest" infra/docker-compose.yml && grep -q "docker compose --env-file ../.env.example up -d" infra/README.md</verify>
  <acceptance_criteria>
    - `infra/docker-compose.yml` contains `mongodb/mongodb-community-server:latest`
    - `infra/docker-compose.yml` contains `qdrant/qdrant:latest`
    - `infra/docker-compose.yml` contains `27017:27017`
    - `infra/docker-compose.yml` contains `6333:6333`
    - `infra/docker-compose.yml` contains `6334:6334`
    - `infra/docker-compose.yml` does not contain `frontend:`
    - `infra/docker-compose.yml` does not contain `backend:`
    - `infra/README.md` contains `docker compose --env-file ../.env.example up -d`
    - `infra/README.md` contains `docker compose down -v`
  </acceptance_criteria>
  <done>Datastore-only Compose and local datastore instructions exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add environment placeholders and root setup notes</name>
  <files>.env.example, README.md</files>
  <read_first>README.md, .gitignore, .planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md</read_first>
  <action>Create `.env.example` with placeholder development values: `MONGODB_ROOT_USERNAME=factchecker`, `MONGODB_ROOT_PASSWORD=factchecker-dev-password`, `MONGODB_URI=mongodb://factchecker:factchecker-dev-password@localhost:27017/fact_checker?authSource=admin`, `MONGODB_DATABASE=fact_checker`, `QDRANT_URL=http://localhost:6333`, `QDRANT_COLLECTION_KNOWLEDGE=fact_checker_knowledge`, `VAULT_ROOT=../vault`, `LOG_LEVEL=INFO`. Update `README.md` to include a `## Local development` section stating that Phase 1 uses a monorepo with `backend/`, `frontend/`, `vault/`, and `infra/`, and that Docker Compose runs MongoDB and Qdrant only by default. Do not include real API keys or provider tokens.</action>
  <verify>grep -q "MONGODB_URI=" .env.example && grep -q "QDRANT_URL=http://localhost:6333" .env.example && grep -q "## Local development" README.md</verify>
  <acceptance_criteria>
    - `.env.example` contains `MONGODB_URI=mongodb://factchecker:factchecker-dev-password@localhost:27017/fact_checker?authSource=admin`
    - `.env.example` contains `QDRANT_URL=http://localhost:6333`
    - `.env.example` contains `VAULT_ROOT=../vault`
    - `.env.example` does not contain `OPENAI_API_KEY=sk-`
    - `README.md` contains `## Local development`
    - `README.md` contains `backend/`
    - `README.md` contains `frontend/`
    - `README.md` contains `vault/`
    - `README.md` contains `infra/`
  </acceptance_criteria>
  <done>Environment placeholders and root setup guidance exist without committed secrets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "mongodb/mongodb-community-server:latest" infra/docker-compose.yml`
- [ ] `grep -q "qdrant/qdrant:latest" infra/docker-compose.yml`
- [ ] `grep -q "MONGODB_URI=" .env.example`
- [ ] `grep -q "## Local development" README.md`
</verification>

<success_criteria>
- Docker Compose defines MongoDB and Qdrant only.
- `.env.example` documents local datastore connection variables without real secrets.
- Root README points to the monorepo structure and local datastore setup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-knowledge-store-contracts/01-01-SUMMARY.md`
</output>
