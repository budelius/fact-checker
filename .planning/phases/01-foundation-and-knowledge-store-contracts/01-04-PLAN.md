---
phase: 01-foundation-and-knowledge-store-contracts
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/data/sampleVault.ts
  - frontend/src/components/AppShell.tsx
  - frontend/src/components/VaultNavigation.tsx
  - frontend/src/components/NotePreview.tsx
  - frontend/src/components/MetadataPanel.tsx
  - frontend/src/styles/tokens.css
  - frontend/src/styles/app.css
autonomous: true
requirements:
  - KB-01
  - KB-02
user_setup: []
must_haves:
  truths:
    - Frontend is React, TypeScript, Vite, and Yarn-oriented.
    - UI shows a static knowledge-browser shell only.
    - UI does not include URL submission, live search, graph visualization, ratings, auth, or report generation.
  artifacts:
    - frontend/package.json exists with Vite scripts and lucide-react dependency.
    - frontend/src/App.tsx renders the shell.
    - frontend/src/components contain app shell, navigation, note preview, and metadata panel.
    - frontend/src/styles contain design tokens from UI-SPEC.
  key_links:
    - Placeholder entity sections match the vault/wiki folders.
    - UI sample frontmatter includes UUID and relationship examples.
---

<objective>
Create the Phase 1 React/Vite knowledge-browser shell.

Purpose: The selected v1 interface includes a website plus Markdown browser. Phase 1 should establish the frontend shell and visual contract without implementing ingestion or live data.
Output: Vite React TypeScript app scaffold with static vault navigation, note preview, metadata panel, design tokens, and responsive layout.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-RESEARCH.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Placeholder UI implies live fact checking exists | medium | Copy and navigation must not expose TikTok submission, real search, graph visualization, or ratings actions in Phase 1. |
| UUID/URL overflow breaks mobile layout | low | CSS must wrap/truncate metadata values inside panels at 320px width. |
| Accessibility regression in navigation | medium | Navigation buttons require accessible labels, focus state, and non-color active marker. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React TypeScript project files</name>
  <files>frontend/package.json, frontend/index.html, frontend/vite.config.ts, frontend/tsconfig.json, frontend/src/main.tsx, frontend/src/App.tsx</files>
  <read_first>.planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md</read_first>
  <action>Create a minimal Vite React TypeScript project manually or via `yarn create vite frontend --template react-ts` if the executor chooses to use the generator. Ensure `frontend/package.json` has scripts exactly `dev`, `build`, `preview`, and `test`, dependencies `@vitejs/plugin-react`, `vite`, `typescript`, `react`, `react-dom`, `lucide-react`, and dev dependency `vitest` if split by package manager. Create `frontend/src/main.tsx` using React `StrictMode` and `createRoot`. Create `frontend/src/App.tsx` rendering `<AppShell />`. Use Yarn as the frontend package manager; do not create npm lockfiles.</action>
  <verify>grep -q '"dev": "vite"' frontend/package.json && grep -q "StrictMode" frontend/src/main.tsx && grep -q "AppShell" frontend/src/App.tsx</verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"dev": "vite"`
    - `frontend/package.json` contains `"build": "vite build"`
    - `frontend/package.json` contains `"preview": "vite preview"`
    - `frontend/package.json` contains `"lucide-react"`
    - `frontend/src/main.tsx` contains `StrictMode`
    - `frontend/src/main.tsx` contains `createRoot`
    - `frontend/src/App.tsx` contains `AppShell`
  </acceptance_criteria>
  <done>Frontend scaffold uses React, TypeScript, Vite, Yarn-compatible scripts, and Strict Mode.</done>
</task>

<task type="auto">
  <name>Task 2: Build static knowledge browser components</name>
  <files>frontend/src/data/sampleVault.ts, frontend/src/components/AppShell.tsx, frontend/src/components/VaultNavigation.tsx, frontend/src/components/NotePreview.tsx, frontend/src/components/MetadataPanel.tsx</files>
  <read_first>.planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md</read_first>
  <action>Create static typed sample data in `frontend/src/data/sampleVault.ts` with sections exactly `Videos`, `Creators`, `Claims`, `Papers`, `Authors`, `Sources`, `Evidence`, `Reports`, `Topics`; include sample note title `attention-is-all-you-need`, sample UUID `00000000-0000-4000-8000-000000000001`, frontmatter keys `uuid`, `entity_type`, `aliases`, `external_ids`, `relationships`, `created_at`, `updated_at`, and wiki links `[[claims/transformer-attention-scaling]]` and `[[authors/ashish-vaswani]]`. Create components `AppShell`, `VaultNavigation`, `NotePreview`, and `MetadataPanel` so the app shows top bar `Fact Checker`, section label `Knowledge Vault`, left navigation, center note preview, and right metadata/relationship panel. Use lucide-react icons for section navigation. Do not add URL submission, search execution, graph visualization, auth, ratings, or report-generation actions.</action>
  <verify>grep -q "attention-is-all-you-need" frontend/src/data/sampleVault.ts && grep -q "Knowledge Vault" frontend/src/components/AppShell.tsx && grep -q "Fact Checker" frontend/src/components/AppShell.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/data/sampleVault.ts` contains `Videos`
    - `frontend/src/data/sampleVault.ts` contains `Creators`
    - `frontend/src/data/sampleVault.ts` contains `Claims`
    - `frontend/src/data/sampleVault.ts` contains `Papers`
    - `frontend/src/data/sampleVault.ts` contains `Evidence`
    - `frontend/src/data/sampleVault.ts` contains `attention-is-all-you-need`
    - `frontend/src/data/sampleVault.ts` contains `00000000-0000-4000-8000-000000000001`
    - `frontend/src/data/sampleVault.ts` contains `[[claims/transformer-attention-scaling]]`
    - `frontend/src/components/AppShell.tsx` contains `Fact Checker`
    - `frontend/src/components/AppShell.tsx` contains `Knowledge Vault`
    - `frontend/src/components/VaultNavigation.tsx` contains `aria-label`
    - `frontend/src/components/MetadataPanel.tsx` contains `relationships`
  </acceptance_criteria>
  <done>Frontend renders a static, accessible knowledge-browser shell aligned to the vault model.</done>
</task>

<task type="auto">
  <name>Task 3: Apply UI-SPEC tokens and responsive layout</name>
  <files>frontend/src/styles/tokens.css, frontend/src/styles/app.css, frontend/src/main.tsx</files>
  <read_first>.planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md, frontend/src/main.tsx</read_first>
  <action>Create `frontend/src/styles/tokens.css` with CSS variables matching UI-SPEC values exactly: `--color-bg: #f8faf7`, `--color-surface: #ffffff`, `--color-ink: #18211f`, `--color-muted: #60706a`, `--color-border: #d8e0dc`, `--color-accent: #256f62`, `--color-accent-soft: #dceee9`, `--color-evidence: #8a5a00`, `--color-destructive: #b42318`, spacing variables `--space-xs: 4px`, `--space-sm: 8px`, `--space-md: 16px`, `--space-lg: 24px`, `--space-xl: 32px`, and layout variables `--topbar-height: 56px`, `--left-rail-width: 248px`, `--right-rail-width: 320px`. Create `frontend/src/styles/app.css` implementing the desktop three-pane layout, tablet collapse, mobile drawer/stack behavior, active nav marker, visible focus states, `letter-spacing: 0`, and wrapping/truncation for long metadata. Import both CSS files from `frontend/src/main.tsx`.</action>
  <verify>grep -q "#f8faf7" frontend/src/styles/tokens.css && grep -q "248px" frontend/src/styles/tokens.css && grep -q "@media" frontend/src/styles/app.css && grep -q "tokens.css" frontend/src/main.tsx</verify>
  <acceptance_criteria>
    - `frontend/src/styles/tokens.css` contains `--color-bg: #f8faf7`
    - `frontend/src/styles/tokens.css` contains `--color-accent: #256f62`
    - `frontend/src/styles/tokens.css` contains `--left-rail-width: 248px`
    - `frontend/src/styles/tokens.css` contains `--right-rail-width: 320px`
    - `frontend/src/styles/tokens.css` contains `--topbar-height: 56px`
    - `frontend/src/styles/app.css` contains `letter-spacing: 0`
    - `frontend/src/styles/app.css` contains `@media`
    - `frontend/src/styles/app.css` contains `overflow-wrap`
    - `frontend/src/main.tsx` contains `./styles/tokens.css`
    - `frontend/src/main.tsx` contains `./styles/app.css`
  </acceptance_criteria>
  <done>UI-SPEC colors, spacing, typography constraints, and responsive layout are encoded in CSS.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q '"dev": "vite"' frontend/package.json`
- [ ] `grep -q "StrictMode" frontend/src/main.tsx`
- [ ] `grep -q "attention-is-all-you-need" frontend/src/data/sampleVault.ts`
- [ ] `grep -q "#256f62" frontend/src/styles/tokens.css`
- [ ] `grep -q "@media" frontend/src/styles/app.css`
</verification>

<success_criteria>
- Frontend app shell exists and is Vite/React/TypeScript/Yarn-oriented.
- Knowledge browser placeholder matches Phase 1 UI-SPEC.
- UI does not expose out-of-scope ingestion/report/search/rating flows.
- CSS encodes the approved color, spacing, typography, and responsive layout contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-knowledge-store-contracts/01-04-SUMMARY.md`
</output>
