---
phase: 01-foundation-and-knowledge-store-contracts
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - backend/app/contracts/__init__.py
  - backend/app/contracts/vault.py
  - backend/app/contracts/store_sync.py
  - backend/tests/test_contracts.py
  - docs/phase-1-contracts.md
  - AGENTS.md
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - KB-04
  - KB-05
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - Cross-store contract code can describe Markdown, MongoDB, and Qdrant identity links without live ingestion.
    - Project instructions mention MongoDB replacing Postgres/Neo4j and link Phase 1 contracts.
    - Contract tests verify required vault folders and key schema fields.
  artifacts:
    - backend/app/contracts/vault.py exists.
    - backend/app/contracts/store_sync.py exists.
    - backend/tests/test_contracts.py exists.
    - docs/phase-1-contracts.md exists.
    - AGENTS.md is updated with Phase 1 contract pointers.
  key_links:
    - Contract code reads vault schema expectations from the same entity names used by backend schemas and frontend sections.
    - docs/phase-1-contracts.md links infrastructure, backend, vault, frontend, and consistency responsibilities.
---

<objective>
Tie Phase 1 artifacts together with consistency contracts and project guidance.

Purpose: Independent foundation work must converge into one contract: UUID identity connects Markdown, MongoDB, and Qdrant; the frontend mirrors the vault model; and later phases know which files define the contract.
Output: backend contract helpers/tests, contract documentation, and updated project guide.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-02-SUMMARY.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-knowledge-store-contracts/01-04-SUMMARY.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Later phases ignore Phase 1 contracts | high | Update `AGENTS.md` and `docs/phase-1-contracts.md` with canonical contract paths. |
| False consistency from live-service-only tests | medium | Contract tests must validate schema/folder/static contracts without requiring MongoDB/Qdrant availability. |
| Relationship drift between Markdown and MongoDB docs | high | Add store-sync contract helpers naming required UUID, vault path, entity type, relationship, and payload fields. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add contract helpers and tests</name>
  <files>backend/app/contracts/__init__.py, backend/app/contracts/vault.py, backend/app/contracts/store_sync.py, backend/tests/test_contracts.py</files>
  <read_first>backend/app/schemas/entities.py, backend/app/schemas/relationships.py, backend/app/schemas/vector_payloads.py, vault/SCHEMA.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md</read_first>
  <action>Create `backend/app/contracts/vault.py` with constants `VAULT_WIKI_ENTITY_FOLDERS = ("videos","creators","claims","papers","authors","sources","evidence","reports","topics")`, `REQUIRED_FRONTMATTER_KEYS = ("uuid","entity_type","slug","title","relationships","created_at","updated_at")`, and function `expected_wiki_path(entity_type: str, slug: str) -> str` returning `vault/wiki/{entity_type}/{slug}.md`. Create `backend/app/contracts/store_sync.py` with constants `MONGODB_ENTITY_COLLECTION = "entities"`, `MONGODB_RELATIONSHIP_COLLECTION = "relationships"`, `QDRANT_REQUIRED_PAYLOAD_KEYS = ("uuid","entity_type","vault_path","chunk_id")`, and function `build_trace_keys(uuid: str, entity_type: str, vault_path: str) -> dict[str, str]` returning exactly those keys. Create `backend/tests/test_contracts.py` that asserts vault folders match the expected tuple, required frontmatter includes `uuid`, Qdrant payload keys include `vault_path`, and `expected_wiki_path("claims", "transformer-attention-scaling")` returns `vault/wiki/claims/transformer-attention-scaling.md`.</action>
  <verify>grep -q "VAULT_WIKI_ENTITY_FOLDERS" backend/app/contracts/vault.py && grep -q "QDRANT_REQUIRED_PAYLOAD_KEYS" backend/app/contracts/store_sync.py && grep -q "test_expected_wiki_path" backend/tests/test_contracts.py</verify>
  <acceptance_criteria>
    - `backend/app/contracts/vault.py` contains `VAULT_WIKI_ENTITY_FOLDERS`
    - `backend/app/contracts/vault.py` contains `expected_wiki_path`
    - `backend/app/contracts/store_sync.py` contains `MONGODB_ENTITY_COLLECTION = "entities"`
    - `backend/app/contracts/store_sync.py` contains `MONGODB_RELATIONSHIP_COLLECTION = "relationships"`
    - `backend/app/contracts/store_sync.py` contains `QDRANT_REQUIRED_PAYLOAD_KEYS`
    - `backend/tests/test_contracts.py` contains `test_expected_wiki_path`
    - `backend/tests/test_contracts.py` contains `vault/wiki/claims/transformer-attention-scaling.md`
  </acceptance_criteria>
  <done>Contract helpers encode vault and store-sync expectations in executable Python tests.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 1 contracts and update agent guidance</name>
  <files>docs/phase-1-contracts.md, AGENTS.md</files>
  <read_first>AGENTS.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-CONTEXT.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-UI-SPEC.md, .planning/phases/01-foundation-and-knowledge-store-contracts/01-RESEARCH.md</read_first>
  <action>Create `docs/phase-1-contracts.md` with sections `## Identity`, `## Markdown Vault`, `## MongoDB`, `## Qdrant`, `## Frontend Shell`, `## Safety`, and `## Out of Scope`. It must state: UUID is canonical identity; Markdown filenames are slug-only; MongoDB stores metadata and relationships; Qdrant stores derived vectors with payload trace keys; frontend is a static knowledge-browser shell; secrets do not go in Markdown/logs; Phase 1 excludes real TikTok/OpenAI/paper API calls. Update `AGENTS.md` to mention Phase 1 contract docs and replace any stale Neo4j/Postgres language with MongoDB + Qdrant. Add a `Phase 1 Contract Files` section listing `docs/phase-1-contracts.md`, `vault/SCHEMA.md`, `backend/app/schemas/entities.py`, `backend/app/schemas/relationships.py`, `backend/app/schemas/vector_payloads.py`, and `backend/app/contracts/store_sync.py`.</action>
  <verify>grep -q "## Identity" docs/phase-1-contracts.md && grep -q "Phase 1 Contract Files" AGENTS.md && grep -q "MongoDB" AGENTS.md && ! grep -q "Neo4j is the initial graph database target" AGENTS.md</verify>
  <acceptance_criteria>
    - `docs/phase-1-contracts.md` contains `## Identity`
    - `docs/phase-1-contracts.md` contains `UUID is canonical identity`
    - `docs/phase-1-contracts.md` contains `Markdown filenames are slug-only`
    - `docs/phase-1-contracts.md` contains `MongoDB stores metadata and relationships`
    - `docs/phase-1-contracts.md` contains `Qdrant stores derived vectors`
    - `docs/phase-1-contracts.md` contains `Phase 1 excludes real TikTok`
    - `AGENTS.md` contains `Phase 1 Contract Files`
    - `AGENTS.md` contains `docs/phase-1-contracts.md`
    - `AGENTS.md` contains `backend/app/contracts/store_sync.py`
    - `AGENTS.md` does not contain `Neo4j is the initial graph database target`
  </acceptance_criteria>
  <done>Project guidance and contract documentation point later agents to the Phase 1 source of truth.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q "VAULT_WIKI_ENTITY_FOLDERS" backend/app/contracts/vault.py`
- [ ] `grep -q "QDRANT_REQUIRED_PAYLOAD_KEYS" backend/app/contracts/store_sync.py`
- [ ] `grep -q "## Identity" docs/phase-1-contracts.md`
- [ ] `grep -q "Phase 1 Contract Files" AGENTS.md`
</verification>

<success_criteria>
- Contract helpers connect vault paths, MongoDB relationship docs, and Qdrant payload trace keys.
- Tests verify static contract expectations without live services.
- Documentation and AGENTS.md tell future agents where Phase 1 contracts live.
- All Phase 1 requirements are covered across the plan set.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-knowledge-store-contracts/01-05-SUMMARY.md`
</output>
