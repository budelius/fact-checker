---
phase: 02-tiktok-ingestion-and-claim-extraction
plan: "06"
type: execute
wave: 4
depends_on:
  - "02-04"
  - "02-05"
files_modified:
  - backend/tests/test_ingestion_e2e.py
  - frontend/src/components/ingestion/fixtures.ts
  - docs/phase-2-ingestion.md
  - AGENTS.md
  - .planning/REQUIREMENTS.md
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04
  - ING-05
  - CLM-01
  - CLM-02
  - CLM-03
  - UI-01
autonomous: true
user_setup: []
must_haves:
  truths:
    - Fixture/pasted-transcript ingestion verifies the full Phase 2 shape without live TikTok/OpenAI access.
    - Requirements remain traceable and are marked complete only after code/tests prove them.
    - Documentation explains the compliance gate, fixture path, and phase boundaries.
  artifacts:
    - Backend E2E fixture test covers submit, progress, transcript, source candidates, and claim/triage artifacts.
    - `docs/phase-2-ingestion.md` documents API routes, progress stages, artifacts, and out-of-scope items.
    - `AGENTS.md` points future agents to Phase 2 context, UI spec, research, and plans.
  key_links:
    - Final verification uses backend tests and frontend build.
    - Phase 3 handoff receives source-candidate artifacts, not evaluated evidence.
---

<objective>
Add final fixture-driven integration coverage and documentation for Phase 2.

Purpose: after backend and frontend pieces exist, prove the phase can run through the owned, testable fixture path and document exactly what Phase 2 delivers and what later phases own.
Output: E2E fixture test, documentation, project guidance updates, and requirements status update.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tiktok-ingestion-and-claim-extraction/02-CONTEXT.md
@.planning/phases/02-tiktok-ingestion-and-claim-extraction/02-UI-SPEC.md
@.planning/phases/02-tiktok-ingestion-and-claim-extraction/02-RESEARCH.md
@.planning/REQUIREMENTS.md
@AGENTS.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| False completion from unit-only tests | medium | Add an E2E fixture test that crosses API, transcript, triage, and job status layers. |
| Docs encourage live download without compliance | high | Documentation must state media download is default-deny and controlled by `TIKTOK_MEDIA_DOWNLOAD_ENABLED`. |
| Requirements marked complete without evidence | medium | Update requirement status only after tests/build commands pass. |
| Future agents blur Phase 2/3/4 scope | medium | AGENTS/docs must state Phase 2 stops at source candidates and pending evidence status. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add fixture E2E coverage for the Phase 2 pipeline</name>
  <files>backend/tests/test_ingestion_e2e.py, frontend/src/components/ingestion/fixtures.ts</files>
  <read_first>backend/app/api/ingestion.py, backend/app/ingestion/transcript.py, backend/app/ingestion/research_basis.py, backend/app/ingestion/claims.py, frontend/src/components/ingestion/fixtures.ts, .planning/phases/02-tiktok-ingestion-and-claim-extraction/02-CONTEXT.md</read_first>
  <action>Create `backend/tests/test_ingestion_e2e.py` using FastAPI `TestClient`. Submit `POST /ingestion/fixtures/transcript` with `source_url` `https://www.tiktok.com/@fixture/video/1234567890` and transcript text containing two timestamp-like lines, an AI research claim, and `arXiv:1706.03762`. Fetch the returned job via `GET /ingestion/jobs/{job_uuid}`. Assert the response has a UUID-like `job_uuid`, status `succeeded` or `running` only if execution remains async, includes stages `validate_url`, `build_transcript`, `triage_research_basis`, and includes a research-basis artifact with candidate count greater than zero. If claim extraction is provider-disabled, assert `extract_claims` stage is `skipped` with message `claim_extraction_provider_disabled`. Extend frontend fixtures so `completeResearchBackedJobFixture` contains a matching arXiv candidate and two sample claims with `evidence_status: pending`.</action>
  <verify>cd backend && uv run pytest tests/test_ingestion_e2e.py -q && cd ../frontend && yarn build</verify>
  <acceptance_criteria>
    - `backend/tests/test_ingestion_e2e.py` contains `test_fixture_transcript_e2e_research_backed_flow`
    - `backend/tests/test_ingestion_e2e.py` contains `POST`
    - `backend/tests/test_ingestion_e2e.py` contains `/ingestion/fixtures/transcript`
    - `backend/tests/test_ingestion_e2e.py` contains `1706.03762`
    - `backend/tests/test_ingestion_e2e.py` contains `claim_extraction_provider_disabled`
    - `frontend/src/components/ingestion/fixtures.ts` contains `arXiv`
    - `frontend/src/components/ingestion/fixtures.ts` contains `evidence_status: "pending"`
    - `cd backend && uv run pytest tests/test_ingestion_e2e.py -q` exits 0
    - `cd frontend && yarn build` exits 0
  </acceptance_criteria>
  <done>Fixture E2E path verifies Phase 2 behavior without live TikTok/OpenAI access.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 2 ingestion behavior and boundaries</name>
  <files>docs/phase-2-ingestion.md, AGENTS.md</files>
  <read_first>AGENTS.md, docs/phase-1-contracts.md, .planning/phases/02-tiktok-ingestion-and-claim-extraction/02-CONTEXT.md, .planning/phases/02-tiktok-ingestion-and-claim-extraction/02-UI-SPEC.md, .planning/phases/02-tiktok-ingestion-and-claim-extraction/02-RESEARCH.md</read_first>
  <action>Create `docs/phase-2-ingestion.md` with sections `## Scope`, `## API Routes`, `## Progress Stages`, `## Artifact Types`, `## Compliance Gate`, `## Fixture And Pasted Transcript Path`, `## Research-Basis Triage`, `## Out Of Scope`, and `## Verification Commands`. It must list routes `POST /ingestion/tiktok`, `POST /ingestion/fixtures/transcript`, `GET /ingestion/jobs/{job_uuid}`; list stage names `validate_url`, `read_public_metadata`, `build_transcript`, `capture_source_clues`, `extract_claims`, `triage_research_basis`, `write_owned_artifacts`; state that `TIKTOK_MEDIA_DOWNLOAD_ENABLED=false` is default; state `evidence_status` remains `pending`; and state paper discovery/evaluation/ratings are out of scope. Update `AGENTS.md` current focus to Phase 2 completion guidance and add a `Phase 2 Contract Files` section listing `docs/phase-2-ingestion.md`, `02-CONTEXT.md`, `02-UI-SPEC.md`, `02-RESEARCH.md`, and `backend/app/schemas/ingestion.py`.</action>
  <verify>grep -q "## API Routes" docs/phase-2-ingestion.md && grep -q "Phase 2 Contract Files" AGENTS.md</verify>
  <acceptance_criteria>
    - `docs/phase-2-ingestion.md` contains `POST /ingestion/tiktok`
    - `docs/phase-2-ingestion.md` contains `GET /ingestion/jobs/{job_uuid}`
    - `docs/phase-2-ingestion.md` contains `TIKTOK_MEDIA_DOWNLOAD_ENABLED=false`
    - `docs/phase-2-ingestion.md` contains `evidence_status`
    - `docs/phase-2-ingestion.md` contains `paper discovery`
    - `docs/phase-2-ingestion.md` contains `cd backend && uv run pytest`
    - `docs/phase-2-ingestion.md` contains `cd frontend && yarn build`
    - `AGENTS.md` contains `Phase 2 Contract Files`
    - `AGENTS.md` contains `docs/phase-2-ingestion.md`
    - `AGENTS.md` contains `backend/app/schemas/ingestion.py`
  </acceptance_criteria>
  <done>Documentation and agent guidance describe Phase 2 behavior, verification, and boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 2 requirements complete after verification</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, docs/phase-2-ingestion.md, .planning/phases/02-tiktok-ingestion-and-claim-extraction/02-CONTEXT.md</read_first>
  <action>After backend tests and frontend build pass, update `.planning/REQUIREMENTS.md` checkboxes for `ING-01`, `ING-02`, `ING-03`, `ING-04`, `ING-05`, `CLM-01`, `CLM-02`, `CLM-03`, and `UI-01` from `[ ]` to `[x]`. Update the requirements traceability table rows for those IDs from `Pending` to `Complete`. Do not mark Phase 3 source discovery, Phase 4 evidence evaluation, or Phase 5 ratings/search/graph requirements complete.</action>
  <verify>grep -q "\\[x\\] **ING-01**" .planning/REQUIREMENTS.md && grep -q "| UI-01 | Phase 2 | Complete |" .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `[x] **ING-01**`
    - `.planning/REQUIREMENTS.md` contains `[x] **ING-02**`
    - `.planning/REQUIREMENTS.md` contains `[x] **ING-05**`
    - `.planning/REQUIREMENTS.md` contains `[x] **CLM-01**`
    - `.planning/REQUIREMENTS.md` contains `[x] **CLM-03**`
    - `.planning/REQUIREMENTS.md` contains `[x] **UI-01**`
    - `.planning/REQUIREMENTS.md` contains `| ING-01 | Phase 2 | Complete |`
    - `.planning/REQUIREMENTS.md` contains `| UI-01 | Phase 2 | Complete |`
    - `.planning/REQUIREMENTS.md` still contains `[ ] **SRC-01**`
    - `.planning/REQUIREMENTS.md` still contains `[ ] **EVAL-01**`
  </acceptance_criteria>
  <done>Requirement status reflects verified Phase 2 completion only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && uv run pytest -q`
- [ ] `cd frontend && yarn build`
- [ ] `grep -q "POST /ingestion/tiktok" docs/phase-2-ingestion.md`
- [ ] `grep -q "Phase 2 Contract Files" AGENTS.md`
- [ ] `grep -q "\\[x\\] **ING-01**" .planning/REQUIREMENTS.md`
- [ ] `grep -q "\\[ \\] **SRC-01**" .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- A fixture/pasted-transcript path proves the Phase 2 pipeline shape end to end.
- Documentation explains API routes, progress stages, artifact types, compliance behavior, triage states, and out-of-scope later phases.
- Requirements are marked complete only for Phase 2 IDs after verification commands pass.
- Backend test suite and frontend build pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tiktok-ingestion-and-claim-extraction/02-06-SUMMARY.md`.
</output>
