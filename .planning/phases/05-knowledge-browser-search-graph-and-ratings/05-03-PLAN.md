---
phase: 05-knowledge-browser-search-graph-and-ratings
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/knowledge/graph.py
  - backend/app/knowledge/consistency.py
  - backend/app/api/knowledge_graph.py
  - backend/tests/test_knowledge_graph.py
  - backend/tests/test_knowledge_consistency.py
autonomous: true
requirements: [KB-06, UI-05]
user_setup: []
must_haves:
  truths:
    - Graph inspection uses MongoDB relationships and vault/Qdrant traceability, not Graphify runtime output.
    - Consistency checks report drift across Markdown, MongoDB, and Qdrant without auto-repairing stores.
    - Graph and consistency responses are useful for explaining why entities are connected.
  artifacts:
    - Graph read model service and route.
    - Consistency check service and route.
    - Tests for graph nodes/edges and drift issue rows.
  key_links:
    - Depends on Plan 01 shared schemas and repository read helpers.
    - Feeds Plan 05 graph and consistency panels.
---

<objective>
Add graph inspection and consistency checking across Markdown, MongoDB, and Qdrant.

Purpose: Phase 5 needs relationship context as a second signal and an actionable drift view so the owned knowledge base remains trustworthy.
Output: `/knowledge/graph/{entity_uuid}` and `/knowledge/consistency` routes with typed graph and drift payloads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-knowledge-browser-search-graph-and-ratings/05-CONTEXT.md
@.planning/phases/05-knowledge-browser-search-graph-and-ratings/05-UI-SPEC.md
@.planning/phases/05-knowledge-browser-search-graph-and-ratings/05-RESEARCH.md
@.planning/phases/05-knowledge-browser-search-graph-and-ratings/05-PATTERNS.md
@backend/app/schemas/entities.py
@backend/app/schemas/relationships.py
@backend/app/schemas/vector_payloads.py
@backend/app/schemas/knowledge.py
@backend/app/repositories/mongo.py
@backend/app/repositories/qdrant.py
@backend/app/knowledge/vault.py
@backend/app/api/knowledge_graph.py
@vault/SCHEMA.md
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Graph view becomes decorative and untraceable | high | Build graph nodes and edges from `KnowledgeEntity` and `KnowledgeRelationship` with provenance, UUIDs, and vault paths. |
| Store drift remains invisible | high | Add `/knowledge/consistency` with counts and issue rows for Markdown, MongoDB, and Qdrant mismatches. |
| Consistency endpoint mutates data unexpectedly | medium | Report suggested repair direction only; do not auto-write repairs in Phase 5. |
| Graphify is accidentally introduced as a runtime dependency | medium | Do not import or install Graphify; use it only as inspiration in docs/plans. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement graph read model</name>
  <files>backend/app/knowledge/graph.py, backend/tests/test_knowledge_graph.py</files>
  <read_first>backend/app/schemas/knowledge.py, backend/app/schemas/entities.py, backend/app/schemas/relationships.py, backend/app/repositories/mongo.py, .planning/phases/05-knowledge-browser-search-graph-and-ratings/05-CONTEXT.md</read_first>
  <action>Create `backend/app/knowledge/graph.py`. Implement `build_entity_graph(entity_uuid: UUID, repository, depth: int = 1) -> KnowledgeGraphResponse`. Use repository entities and relationships to build nodes with UUID, entity type, title, vault path, and degree, plus edges with relationship UUID, relationship type, source UUID, target UUID, provenance, and direction relative to the selected entity. Include optional `important_nodes` as the top three nodes by degree and optional `clusters` grouped by entity type. Do not call Graphify or read Graphify output.</action>
  <verify>cd backend && uv run pytest tests/test_knowledge_graph.py -q</verify>
  <acceptance_criteria>
    - `backend/app/knowledge/graph.py` contains `def build_entity_graph`
    - `backend/app/knowledge/graph.py` contains `KnowledgeGraphResponse`
    - `backend/app/knowledge/graph.py` contains `important_nodes`
    - `backend/app/knowledge/graph.py` contains `clusters`
    - `backend/app/knowledge/graph.py` does not contain `graphify`
    - `backend/tests/test_knowledge_graph.py` asserts a `supports` edge is returned
    - `backend/tests/test_knowledge_graph.py` asserts node UUIDs are present
    - `cd backend && uv run pytest tests/test_knowledge_graph.py -q` exits 0
  </acceptance_criteria>
  <done>Selected entities can be explained through graph nodes and edges.</done>
</task>

<task type="auto">
  <name>Task 2: Implement consistency check service</name>
  <files>backend/app/knowledge/consistency.py, backend/tests/test_knowledge_consistency.py</files>
  <read_first>backend/app/knowledge/vault.py, backend/app/repositories/mongo.py, backend/app/repositories/qdrant.py, backend/app/schemas/knowledge.py, backend/app/schemas/vector_payloads.py, vault/SCHEMA.md</read_first>
  <action>Create `backend/app/knowledge/consistency.py`. Implement `check_knowledge_consistency(vault_root: Path, repository, qdrant_repository) -> KnowledgeConsistencySummary`. Compare parsed Markdown notes against Mongo entities by UUID and vault path, compare wiki links or relationship frontmatter against Mongo relationships when UUIDs are available, compare Qdrant payload UUID/vault path records from `scroll_payloads`, and report issue rows with `status` values only `synced`, `drift`, or `broken`. Include summary counts for `checked_notes`, `missing_mongo_records`, `missing_qdrant_payloads`, `broken_relationships`, and `orphan_vectors`. Each issue row must include affected UUID where known, entity type where known, store, issue text, and suggested repair direction. Do not mutate files, MongoDB, or Qdrant.</action>
  <verify>cd backend && uv run pytest tests/test_knowledge_consistency.py -q</verify>
  <acceptance_criteria>
    - `backend/app/knowledge/consistency.py` contains `def check_knowledge_consistency`
    - `backend/app/knowledge/consistency.py` contains `missing_mongo_records`
    - `backend/app/knowledge/consistency.py` contains `missing_qdrant_payloads`
    - `backend/app/knowledge/consistency.py` contains `broken_relationships`
    - `backend/app/knowledge/consistency.py` contains `orphan_vectors`
    - `backend/app/knowledge/consistency.py` contains `suggested_repair`
    - `backend/tests/test_knowledge_consistency.py` asserts `status` equals `drift` for a non-blocking mismatch
    - `backend/tests/test_knowledge_consistency.py` asserts `status` equals `broken` for a missing canonical record
    - `cd backend && uv run pytest tests/test_knowledge_consistency.py -q` exits 0
  </acceptance_criteria>
  <done>Store drift can be reported without auto-repairing data.</done>
</task>

<task type="auto">
  <name>Task 3: Expose graph and consistency API routes</name>
  <files>backend/app/api/knowledge_graph.py, backend/tests/test_knowledge_graph.py, backend/tests/test_knowledge_consistency.py</files>
  <read_first>backend/app/api/knowledge_graph.py, backend/app/api/knowledge_browse.py, backend/app/knowledge/graph.py, backend/app/knowledge/consistency.py, backend/app/schemas/knowledge.py</read_first>
  <action>Replace the placeholder subrouter in `backend/app/api/knowledge_graph.py` with `GET /graph/{entity_uuid}` and `GET /consistency`. `GET /graph/{entity_uuid}` accepts `depth` defaulting to 1 and returns `KnowledgeGraphResponse`. `GET /consistency` runs `check_knowledge_consistency` and returns the summary payload. Reuse Plan 01 dependency patterns for repository, Qdrant repository, and vault root.</action>
  <verify>cd backend && uv run pytest tests/test_knowledge_graph.py tests/test_knowledge_consistency.py tests/test_knowledge_api.py -q</verify>
  <acceptance_criteria>
    - `backend/app/api/knowledge_graph.py` contains `@router.get("/graph/{entity_uuid}")`
    - `backend/app/api/knowledge_graph.py` contains `@router.get("/consistency")`
    - `backend/app/api/knowledge_graph.py` contains `check_knowledge_consistency`
    - `backend/app/api/knowledge_graph.py` contains `build_entity_graph`
    - `backend/tests/test_knowledge_graph.py` asserts `GET /knowledge/graph/`
    - `backend/tests/test_knowledge_consistency.py` asserts `GET /knowledge/consistency`
    - `cd backend && uv run pytest tests/test_knowledge_graph.py tests/test_knowledge_consistency.py tests/test_knowledge_api.py -q` exits 0
  </acceptance_criteria>
  <done>Graph and consistency data are available to the frontend.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cd backend && uv run pytest tests/test_knowledge_graph.py tests/test_knowledge_consistency.py tests/test_knowledge_api.py -q`
- [ ] `rg -n "def build_entity_graph|def check_knowledge_consistency|@router.get\\(\"/graph/\\{entity_uuid\\}\"\\)|@router.get\\(\"/consistency\"\\)" backend/app backend/tests`
- [ ] `rg -n "synced|drift|broken|orphan_vectors|important_nodes|clusters" backend/app/knowledge backend/tests`
</verification>

<success_criteria>
- Graph inspection returns nodes, edges, and provenance for selected entities.
- Consistency checks compare Markdown, MongoDB, and Qdrant payload metadata.
- Drift is actionable but not auto-repaired.
- No Graphify runtime dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-browser-search-graph-and-ratings/05-03-SUMMARY.md`.
</output>

